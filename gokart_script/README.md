# Gokart Rental - Stark_HUN
Professzionális, moduláris bérlőrendszer.

## Telepítés
1. Bizonyosodj meg róla, hogy a `qb-core` vagy `es_extended` fut.
2. Állítsd be a `Config.TargetSystem`-et a `config.lua` fájlban.
3. Importáld az `ox_target` vagy `qb-target` exportokat a `client.lua` fájlba.